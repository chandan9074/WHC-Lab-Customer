const LabelText = ({ children }) => {
    return (
        <label className="text-[#5D6B82] font-semibold text-sm font-poppins">
            {children}
        </label>
    );
};

export default LabelText;
